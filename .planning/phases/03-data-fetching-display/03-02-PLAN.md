---
phase: 03-data-fetching-display
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/renderer/src/components/ProgressBar.tsx
  - src/renderer/src/components/ProgressBar.css
  - src/renderer/src/components/UsageDisplay.tsx
  - src/renderer/src/components/UsageDisplay.css
autonomous: true

must_haves:
  truths:
    - "Progress bars show smooth gradient that transitions green to yellow to red"
    - "Each progress bar displays percentage, label, and reset time"
    - "Most-limiting metric is visually highlighted and positioned first"
    - "Hover on progress bar shows absolute values in tooltip"
    - "Hover on reset time shows relative duration"
  artifacts:
    - path: "src/renderer/src/components/ProgressBar.tsx"
      provides: "Reusable progress bar component with gradient colors"
      exports: ["ProgressBar"]
    - path: "src/renderer/src/components/ProgressBar.css"
      provides: "Progress bar styling with track, fill, and hover states"
      contains: ".progress-bar"
    - path: "src/renderer/src/components/UsageDisplay.tsx"
      provides: "Container showing three usage limits sorted by severity"
      exports: ["UsageDisplay"]
    - path: "src/renderer/src/components/UsageDisplay.css"
      provides: "Usage display layout and highlighting for most-limiting"
      contains: ".usage-display"
  key_links:
    - from: "src/renderer/src/components/UsageDisplay.tsx"
      to: "src/renderer/src/components/ProgressBar.tsx"
      via: "import ProgressBar"
      pattern: "import.*ProgressBar"
---

<objective>
Create the UI components for displaying usage data with progress bars and visual indicators.

Purpose: Build reusable, well-styled React components that display usage limits with gradient progress bars, reset time countdowns, and visual emphasis on the most-limiting constraint. These components are pure UI - they receive data as props and render it.

Output:
- ProgressBar component with smooth gradient colors (green→yellow→red)
- UsageDisplay component that shows three progress bars with proper sorting
- CSS styling for dark theme compatibility and hover interactions
</objective>

<execution_context>
@C:\Users\Andrew\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\Andrew\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-data-fetching-display/03-CONTEXT.md
@.planning/phases/03-data-fetching-display/03-RESEARCH.md
@src/renderer/src/App.tsx
@src/renderer/src/App.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ProgressBar component with gradient styling</name>
  <files>
    src/renderer/src/components/ProgressBar.tsx
    src/renderer/src/components/ProgressBar.css
  </files>
  <action>
1. Create directory: `src/renderer/src/components/` if it doesn't exist

2. Create `src/renderer/src/components/ProgressBar.tsx`:
```typescript
import React from 'react'
import { format, formatDistanceToNow } from 'date-fns'
import './ProgressBar.css'

interface ProgressBarProps {
  label: string
  percentage: number
  current: number
  total: number
  resetAt: Date
  isLimiting?: boolean  // True for most-limiting constraint
}

export function ProgressBar({
  label,
  percentage,
  current,
  total,
  resetAt,
  isLimiting = false
}: ProgressBarProps) {
  // Calculate gradient based on percentage thresholds
  // Green < 70%, Yellow 70-90%, Red >= 90%
  const getGradient = (pct: number): string => {
    if (pct < 70) {
      // All green
      return 'linear-gradient(to right, #22c55e, #22c55e)'
    } else if (pct < 90) {
      // Transition from green to yellow
      const greenEnd = Math.round((70 / pct) * 100)
      return `linear-gradient(to right, #22c55e ${greenEnd}%, #eab308 100%)`
    } else {
      // Full transition: green -> yellow -> red
      const greenEnd = Math.round((70 / pct) * 100)
      const yellowEnd = Math.round((90 / pct) * 100)
      return `linear-gradient(to right, #22c55e ${greenEnd}%, #eab308 ${yellowEnd}%, #ef4444 100%)`
    }
  }

  // Format reset time
  const absoluteTime = format(resetAt, 'h:mm a')  // "3:00 PM"
  const relativeTime = formatDistanceToNow(resetAt, { addSuffix: true })  // "in 2 hours"

  // Tooltip text for absolute values
  const tooltipText = `${current} / ${total}`

  return (
    <div className={`progress-bar-container ${isLimiting ? 'limiting' : ''}`}>
      <div className="progress-header">
        <span className="progress-label">{label}</span>
        <span className="progress-percentage">{percentage}%</span>
      </div>

      <div className="progress-track" title={tooltipText}>
        <div
          className="progress-fill"
          style={{
            width: `${Math.min(percentage, 100)}%`,
            background: getGradient(percentage)
          }}
        />
      </div>

      <div className="progress-footer">
        <span className="reset-time" title={relativeTime}>
          Resets at {absoluteTime}
        </span>
      </div>
    </div>
  )
}
```

3. Create `src/renderer/src/components/ProgressBar.css`:
```css
.progress-bar-container {
  padding: 8px 12px;
  border-radius: 6px;
  background: rgba(255, 255, 255, 0.03);
  transition: background 0.2s ease;
}

.progress-bar-container:hover {
  background: rgba(255, 255, 255, 0.06);
}

/* Most-limiting constraint highlight */
.progress-bar-container.limiting {
  background: rgba(255, 255, 255, 0.08);
  border-left: 3px solid #eab308;
  padding-left: 9px;
}

.progress-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 6px;
}

.progress-label {
  font-size: 13px;
  color: #e0e0e0;
  font-weight: 500;
}

.progress-percentage {
  font-size: 14px;
  color: #ffffff;
  font-weight: 600;
  font-variant-numeric: tabular-nums;
}

.progress-track {
  height: 8px;
  background: rgba(255, 255, 255, 0.1);
  border-radius: 4px;
  overflow: hidden;
  cursor: help;
}

.progress-fill {
  height: 100%;
  border-radius: 4px;
  transition: width 0.3s ease;
}

.progress-footer {
  margin-top: 4px;
}

.reset-time {
  font-size: 11px;
  color: #9ca3af;
  cursor: help;
}
```
  </action>
  <verify>
- `src/renderer/src/components/ProgressBar.tsx` exists and exports ProgressBar
- `src/renderer/src/components/ProgressBar.css` exists with styling
- TypeScript compilation succeeds: `npx tsc --noEmit`
- Component can be imported in App.tsx without errors
  </verify>
  <done>
- ProgressBar component renders gradient progress based on percentage
- Colors transition: green (0-70%), green→yellow (70-90%), green→yellow→red (90%+)
- Hover tooltip shows absolute values (current/total)
- Reset time shows absolute time with relative duration on hover
- Most-limiting state applies visual highlight (border + background)
  </done>
</task>

<task type="auto">
  <name>Task 2: Create UsageDisplay component with three progress bars</name>
  <files>
    src/renderer/src/components/UsageDisplay.tsx
    src/renderer/src/components/UsageDisplay.css
  </files>
  <action>
1. Create `src/renderer/src/components/UsageDisplay.tsx`:
```typescript
import React from 'react'
import { formatDistanceToNow } from 'date-fns'
import { ProgressBar } from './ProgressBar'
import './UsageDisplay.css'

// Types matching the API types
interface UsageLimit {
  current: number
  total: number
  percentage: number
  resetAt: string  // ISO timestamp
}

interface UsageData {
  sessionLimit: UsageLimit
  weeklyAllModels: UsageLimit
  weeklySonnet: UsageLimit
  fetchedAt: string
}

interface UsageDisplayProps {
  data: UsageData | null
  lastUpdated: Date | null
  error: string | null
  isLoading: boolean
  onRefresh: () => void
}

// Human-readable labels for each limit type
const LIMIT_LABELS: Record<string, string> = {
  sessionLimit: 'Session Limit',
  weeklyAllModels: 'Weekly (All Models)',
  weeklySonnet: 'Weekly (Sonnet)'
}

export function UsageDisplay({
  data,
  lastUpdated,
  error,
  isLoading,
  onRefresh
}: UsageDisplayProps) {
  // Loading state
  if (isLoading && !data) {
    return (
      <div className="usage-display">
        <div className="loading-state">Loading usage data...</div>
      </div>
    )
  }

  // No data state
  if (!data) {
    return (
      <div className="usage-display">
        <div className="no-data-state">
          <p>No usage data available.</p>
          <button className="refresh-button" onClick={onRefresh} disabled={isLoading}>
            {isLoading ? 'Loading...' : 'Load Usage Data'}
          </button>
        </div>
      </div>
    )
  }

  // Sort limits by percentage (most-limiting first)
  const limits = [
    { key: 'sessionLimit', ...data.sessionLimit },
    { key: 'weeklyAllModels', ...data.weeklyAllModels },
    { key: 'weeklySonnet', ...data.weeklySonnet }
  ].sort((a, b) => b.percentage - a.percentage)

  // The first one after sorting is the most limiting
  const mostLimitingKey = limits[0].key

  return (
    <div className="usage-display">
      {/* Error indicator (shows stale data warning) */}
      {error && (
        <div className="stale-indicator">
          Could not refresh. {lastUpdated && (
            <span>Last updated {formatDistanceToNow(lastUpdated, { addSuffix: true })}</span>
          )}
        </div>
      )}

      {/* Progress bars for each limit */}
      <div className="usage-limits">
        {limits.map((limit) => (
          <ProgressBar
            key={limit.key}
            label={LIMIT_LABELS[limit.key]}
            percentage={limit.percentage}
            current={limit.current}
            total={limit.total}
            resetAt={new Date(limit.resetAt)}
            isLimiting={limit.key === mostLimitingKey}
          />
        ))}
      </div>

      {/* Refresh button */}
      <div className="usage-footer">
        <button
          className="refresh-button"
          onClick={onRefresh}
          disabled={isLoading}
        >
          {isLoading ? 'Refreshing...' : 'Refresh'}
        </button>
      </div>
    </div>
  )
}
```

2. Create `src/renderer/src/components/UsageDisplay.css`:
```css
.usage-display {
  display: flex;
  flex-direction: column;
  gap: 8px;
  padding: 4px 0;
}

.loading-state,
.no-data-state {
  text-align: center;
  padding: 20px;
  color: #9ca3af;
}

.no-data-state p {
  margin-bottom: 12px;
}

.stale-indicator {
  background: rgba(234, 179, 8, 0.15);
  border: 1px solid rgba(234, 179, 8, 0.3);
  border-radius: 4px;
  padding: 8px 12px;
  font-size: 12px;
  color: #eab308;
  margin-bottom: 4px;
}

.usage-limits {
  display: flex;
  flex-direction: column;
  gap: 8px;
}

.usage-footer {
  display: flex;
  justify-content: center;
  padding-top: 8px;
  border-top: 1px solid rgba(255, 255, 255, 0.1);
  margin-top: 4px;
}

.refresh-button {
  background: rgba(255, 255, 255, 0.1);
  border: none;
  border-radius: 4px;
  padding: 6px 16px;
  font-size: 12px;
  color: #e0e0e0;
  cursor: pointer;
  transition: background 0.2s ease;
}

.refresh-button:hover:not(:disabled) {
  background: rgba(255, 255, 255, 0.15);
}

.refresh-button:disabled {
  opacity: 0.5;
  cursor: not-allowed;
}
```
  </action>
  <verify>
- `src/renderer/src/components/UsageDisplay.tsx` exists and exports UsageDisplay
- `src/renderer/src/components/UsageDisplay.css` exists with styling
- TypeScript compilation succeeds: `npx tsc --noEmit`
- UsageDisplay imports ProgressBar without errors
  </verify>
  <done>
- UsageDisplay component renders three progress bars for usage limits
- Limits are sorted by percentage (most-limiting first)
- Most-limiting constraint is highlighted via isLimiting prop
- Stale data indicator shows when error occurred with last update time
- Refresh button allows manual data refresh
- Loading states handled appropriately
  </done>
</task>

</tasks>

<verification>
1. Run `npx tsc --noEmit` to verify TypeScript compilation
2. Verify both components can be imported:
   ```typescript
   import { ProgressBar } from './components/ProgressBar'
   import { UsageDisplay } from './components/UsageDisplay'
   ```
3. Check that CSS files are properly structured for dark theme
4. Verify gradient calculation produces correct values at boundary percentages (0%, 70%, 90%, 100%)
</verification>

<success_criteria>
- ProgressBar component renders with gradient colors based on percentage thresholds
- Hover interactions show tooltips with additional information
- UsageDisplay sorts and renders three progress bars with most-limiting first
- Visual highlighting applied to most-limiting constraint
- Stale data indicator shows when refresh fails
- All components compile without TypeScript errors
- Styling matches dark theme from existing App.css
</success_criteria>

<output>
After completion, create `.planning/phases/03-data-fetching-display/03-02-SUMMARY.md`
</output>
