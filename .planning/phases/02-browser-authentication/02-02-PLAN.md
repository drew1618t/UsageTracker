---
phase: 02-browser-authentication
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/renderer/src/App.tsx
  - src/renderer/src/App.css
autonomous: false

must_haves:
  truths:
    - "Popup shows 'Log in to see usage' with login button when not authenticated"
    - "After successful login, popup shows 'Logged in as [identifier]'"
    - "Login button in popup triggers browser login flow"
    - "Auth state persists across app restart"
    - "Session expiry detection prompts re-authentication"
  artifacts:
    - path: "src/renderer/src/App.tsx"
      provides: "Auth-aware UI with login button and status display"
      contains: "useEffect.*onAuthStateChanged"
    - path: "src/renderer/src/App.css"
      provides: "Styles for login button and auth status"
      contains: "login-button"
  key_links:
    - from: "src/renderer/src/App.tsx"
      to: "window.electronAPI.getAuthState"
      via: "Initial auth check on mount"
      pattern: "getAuthState\\(\\)"
    - from: "src/renderer/src/App.tsx"
      to: "window.electronAPI.onAuthStateChanged"
      via: "Live auth state subscription"
      pattern: "onAuthStateChanged"
    - from: "src/renderer/src/App.tsx"
      to: "window.electronAPI.login"
      via: "Login button click handler"
      pattern: "onClick.*login\\(\\)"
---

<objective>
Implement auth-aware popup UI and verify the complete authentication flow.

Purpose: Give users a clear visual indication of auth state and a way to log in from the popup window. Verify the full flow works end-to-end.

Output: Updated popup UI that responds to auth state, with human verification of the complete login/persistence/re-auth flow.
</objective>

<execution_context>
@C:\Users\Andrew\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\Andrew\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-browser-authentication/02-CONTEXT.md
@.planning/phases/02-browser-authentication/02-01-SUMMARY.md

@src/renderer/src/App.tsx
@src/renderer/src/App.css
@src/preload/index.ts
@src/preload/index.d.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update popup UI for auth state</name>
  <files>
    src/renderer/src/App.tsx
    src/renderer/src/App.css
  </files>
  <action>
**Update src/renderer/src/App.tsx:**
- Add auth state: `const [authState, setAuthState] = useState<{ isAuthenticated: boolean; userIdentifier: string | null }>({ isAuthenticated: false, userIdentifier: null })`
- Add loading state: `const [isLoggingIn, setIsLoggingIn] = useState(false)`
- On mount useEffect:
  - Call `window.electronAPI.getAuthState()` and setAuthState with result
  - Subscribe to `window.electronAPI.onAuthStateChanged((state) => { setAuthState(state); setIsLoggingIn(false) })`
  - Return cleanup function from onAuthStateChanged
- Handle login click:
  - `const handleLogin = async () => { setIsLoggingIn(true); await window.electronAPI.login() }`
  - Note: isLoggingIn remains true until auth-state-changed event arrives
- Render conditionally:
  - If `isLoggingIn`: Show "Waiting for login..." message with subtle animation
  - If `!authState.isAuthenticated`:
    - Main content: "Log in to see usage" message
    - Login button: "Log in with Claude.ai" (styled prominently)
    - Footer: Remove "Click 'Login' from tray menu" text
  - If `authState.isAuthenticated`:
    - Header: Add user indicator (e.g., small "Logged in" badge or show userIdentifier if available)
    - Main content: Keep placeholder "No usage data yet" (Phase 3 will add actual data)
    - Footer: Can show "Logged in" or just be minimal

**Update src/renderer/src/App.css:**
- Add `.login-button`:
  - Background: #5865F2 (Discord-like blue) or #6366f1 (Tailwind indigo-500)
  - Color: white, padding: 12px 24px, border-radius: 8px
  - Font-weight: 600, cursor: pointer
  - Hover: slightly lighter background
  - No border
- Add `.login-button:disabled`:
  - Opacity: 0.6, cursor: not-allowed
- Add `.waiting-message`:
  - Animated ellipsis or subtle pulse
- Add `.user-badge`:
  - Small pill showing login status in header
  - Background: #2d2d44 (matching dark theme), color: #888, font-size: 11px
  </action>
  <verify>
Run `npm run dev`. Click tray icon. Popup should show login button with "Log in to see usage" message.
  </verify>
  <done>
Popup UI displays correct state for logged-out users with prominent login button. Clicking login triggers browser flow.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Complete browser authentication flow:
1. Gray tray icon and "Login" menu when logged out
2. Login button in popup window
3. Browser opens to Claude.ai login page
4. Cookie detection updates auth state after login
5. Session persistence across app restart
6. Logout functionality from context menu
  </what-built>
  <how-to-verify>
**Test 1: Initial State (Logged Out)**
1. Run `npm run dev`
2. Observe tray icon is GRAY (not green)
3. Click tray icon - popup shows "Log in to see usage" with login button
4. Right-click tray - context menu shows "Login" option

**Test 2: Login Flow**
1. Click the login button in popup OR "Login" from context menu
2. System browser should open to https://claude.ai/login
3. Complete login in browser (use your Claude.ai account)
4. After login completes in browser, return to app
5. Popup should now show "Logged in" (or email if detected)
6. Tray icon should change from gray to green
7. Right-click tray - context menu should now show "Logout" instead of "Login"

**Test 3: Session Persistence**
1. Close the app completely (Exit from tray menu)
2. Restart app with `npm run dev`
3. App should remember login - tray icon should be green immediately
4. Popup should show logged-in state without requiring login again

**Test 4: Logout Flow**
1. Right-click tray, select "Logout"
2. Tray icon should return to gray
3. Popup should show "Log in to see usage" again

**Test 5: Re-auth Detection (Optional - may need to wait for session expiry)**
If you can force session expiry or clear cookies externally:
1. The app should detect logged-out state on next check
2. Tray should return to gray and prompt for login

**Expected Outcomes:**
- All 4 main tests pass
- Login feels native (opens real browser, not embedded)
- State transitions are immediate (no lag after browser login)
  </how-to-verify>
  <resume-signal>Type "approved" if all tests pass, or describe specific issues encountered</resume-signal>
</task>

</tasks>

<verification>
Phase 2 success criteria from ROADMAP.md:
1. [x] User can click Login option and browser window opens to Claude.ai sign-in page
2. [x] After user completes login, app shows "Logged in as [identifier]" in popup
3. [x] User closes app and relaunches without needing to log in again (session persists)
4. [x] When session expires, app detects this and prompts user to re-authenticate from popup

Requirements addressed:
- AUTH-01: User can log in via browser window that opens Claude.ai sign-in page
- AUTH-02: Authenticated session persists across app restarts
- AUTH-03: App detects expired session and prompts re-authentication
</verification>

<success_criteria>
- Popup shows auth-aware UI (login button when logged out, status when logged in)
- Login button opens Claude.ai in system browser
- Cookie detection automatically updates auth state after browser login
- Tray icon reflects auth state (gray = logged out, green = logged in)
- Context menu shows Login/Logout based on current state
- Session persists across app restart
- Human verification confirms all flows work correctly
</success_criteria>

<output>
After completion, create `.planning/phases/02-browser-authentication/02-02-SUMMARY.md`
</output>
