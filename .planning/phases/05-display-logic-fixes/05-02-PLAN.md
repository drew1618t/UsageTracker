---
phase: 05-display-logic-fixes
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - src/main/tray.ts
  - src/renderer/src/components/UsageDisplay.tsx
autonomous: false

must_haves:
  truths:
    - "Tray tooltip shows session limit by default"
    - "Tray tooltip shows weekly only when >90% AND more limiting"
    - "Tray icon color reflects ANY limit hitting threshold"
    - "Popup shows session first, weekly prioritized when conditions met"
  artifacts:
    - path: "src/main/tray.ts"
      provides: "Updated tray display using new logic"
      contains: "selectPrimaryLimit"
    - path: "src/renderer/src/components/UsageDisplay.tsx"
      provides: "Updated popup display using new logic"
      contains: "selectPrimaryLimit"
  key_links:
    - from: "src/main/tray.ts"
      to: "src/main/utils/displayLogic.ts"
      via: "import"
      pattern: "import.*displayLogic"
    - from: "src/renderer/src/components/UsageDisplay.tsx"
      to: "selectPrimaryLimit logic"
      via: "inline implementation"
      pattern: "weeklyLimit\\.percentage.*90"
---

<objective>
Integrate display logic into tray and popup components

Purpose: Apply session-first display with smart weekly override to both tray (tooltip) and popup (UsageDisplay), and ensure tray icon color reflects ANY limit hitting thresholds
Output: Updated tray.ts and UsageDisplay.tsx using the new display logic
</objective>

<execution_context>
@C:\Users\Andrew\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\Andrew\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-display-logic-fixes/05-RESEARCH.md
@.planning/phases/05-display-logic-fixes/05-01-SUMMARY.md

@src/main/tray.ts
@src/main/utils/displayLogic.ts
@src/renderer/src/components/UsageDisplay.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update tray.ts to use new display logic</name>
  <files>src/main/tray.ts</files>
  <action>
Modify `updateTrayForUsage` function in src/main/tray.ts:

1. Import selectPrimaryLimit and determineTrayIconColor from '../utils/displayLogic'

2. Replace current "most limiting by percentage" logic with:
   - Use selectPrimaryLimit(data.sessionLimit, data.weeklyAllModels) for tooltip
   - Use determineTrayIconColor(data.sessionLimit, data.weeklyAllModels, data.weeklySonnet) for icon

3. Update tooltip format:
   - If type === 'session': `Claude: Session ${percentage}%`
   - If type === 'weekly': `Claude: Weekly ${percentage}%`

4. Keep icon color logic but use the new determineTrayIconColor function result

Remove the old sorting logic that finds "mostLimiting".
  </action>
  <verify>
- TypeScript compiles: `npm run typecheck` passes
- Start app: `npm run dev`
- Tray tooltip shows "Claude: Session X%" by default
- Tray icon reflects highest of ANY limit (check color matches expectations)
  </verify>
  <done>
Tray tooltip shows session-first display, icon color reflects any limit hitting threshold
  </done>
</task>

<task type="auto">
  <name>Task 2: Update UsageDisplay.tsx for session-first display</name>
  <files>src/renderer/src/components/UsageDisplay.tsx</files>
  <action>
Modify UsageDisplay component in src/renderer/src/components/UsageDisplay.tsx:

1. Add inline selectPrimaryLimit logic (renderer can't import from main process):
   ```typescript
   function selectPrimaryLimit(
     sessionLimit: UsageLimit,
     weeklyLimit: UsageLimit
   ): { limit: UsageLimit; type: 'session' | 'weekly' } {
     // Default to session
     if (weeklyLimit.percentage <= 90) {
       return { limit: sessionLimit, type: 'session' }
     }
     // Weekly is >90%, check if more limiting
     const weeklyRemaining = weeklyLimit.total - weeklyLimit.current
     const sessionRemaining = sessionLimit.total - sessionLimit.current
     // 1 session ≈ 10% weekly
     const weeklyRemainingInSessions = weeklyRemaining / (weeklyLimit.total / 10)
     if (weeklyRemainingInSessions < sessionRemaining) {
       return { limit: weeklyLimit, type: 'weekly' }
     }
     return { limit: sessionLimit, type: 'session' }
   }
   ```

2. Replace sorting logic:
   - Instead of sorting all limits by percentage
   - Use selectPrimaryLimit to determine which is "isLimiting"
   - Keep displaying all three bars, but mark the primary one as isLimiting

3. Order bars: Session first, then Weekly (All Models), then Weekly (Sonnet)
   - Fixed order, not sorted by percentage
   - The "isLimiting" prop highlights whichever is the primary limit

Handle edge cases: if weeklyLimit.total === 0, default to session.
  </action>
  <verify>
- TypeScript compiles: `npm run typecheck` passes
- Start app: `npm run dev`
- Popup shows Session Limit first (fixed position)
- Session has yellow border (isLimiting) by default
- If weekly >90% AND more limiting, weekly gets yellow border instead
  </verify>
  <done>
Popup shows session-first display with smart weekly prioritization
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Complete display logic implementation:
- Tray tooltip shows session by default, weekly when >90% AND more limiting
- Tray icon color reflects ANY limit (not just displayed one)
- Popup shows session first with smart highlighting
- Auto-start protected from dev mode registry pollution
  </what-built>
  <how-to-verify>
1. Run `npm run dev` and authenticate if needed

2. Check tray tooltip:
   - Hover over tray icon
   - Should show "Claude: Session X%" (unless weekly is >90% AND more limiting)

3. Check popup display:
   - Click tray icon to open popup
   - Session Limit should be listed first (fixed order)
   - Session should have yellow "isLimiting" border (unless weekly prioritized)

4. Test icon color logic:
   - If your weekly usage is high (e.g., 75%), icon should be yellow
   - Even if session is low, the icon reflects the HIGHEST limit

5. Test auto-start (dev mode):
   - Toggle auto-start in Settings
   - Check console for "[Settings] Skipping auto-start in dev mode"
   - Windows Startup Apps should NOT show new entry (dev mode skipped)

6. Build and test production (optional but recommended):
   - Run `npm run build` then `npm run start`
   - In production, auto-start should work and use correct app path
  </how-to-verify>
  <resume-signal>Type "approved" to complete the phase, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
1. `npm run typecheck` passes
2. Tray tooltip shows session-first format
3. Popup displays session first with correct highlighting
4. Icon color reflects any limit hitting threshold
5. Human verification of UX behavior
</verification>

<success_criteria>
- All DISPLAY requirements (01, 02, 03) verified working
- Auto-start FIX-01 verified in dev mode (production recommended)
- Human approved the behavior matches requirements
</success_criteria>

<output>
After completion, create `.planning/phases/05-display-logic-fixes/05-02-SUMMARY.md`
</output>
