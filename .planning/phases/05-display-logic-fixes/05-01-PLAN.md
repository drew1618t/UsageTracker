---
phase: 05-display-logic-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/utils/displayLogic.ts
  - src/main/state/settings.ts
autonomous: true

must_haves:
  truths:
    - "selectPrimaryLimit returns session by default"
    - "selectPrimaryLimit returns weekly only when >90% AND more limiting"
    - "determineTrayIconColor returns color based on ANY limit hitting threshold"
    - "Auto-start in production uses correct app path not electron.exe"
  artifacts:
    - path: "src/main/utils/displayLogic.ts"
      provides: "Display logic utility functions"
      exports: ["selectPrimaryLimit", "determineTrayIconColor"]
    - path: "src/main/state/settings.ts"
      provides: "Auto-start with dev mode protection"
      contains: "app.isPackaged"
  key_links:
    - from: "src/main/utils/displayLogic.ts"
      to: "UsageData type"
      via: "import"
      pattern: "import.*UsageLimit"
---

<objective>
Create display logic utility functions and fix auto-start bug

Purpose: Establish shared logic for session-first display with smart weekly override, and ensure auto-start only configures when in production (not development)
Output: displayLogic.ts utility module and fixed settings.ts auto-start handling
</objective>

<execution_context>
@C:\Users\Andrew\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\Andrew\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-display-logic-fixes/05-RESEARCH.md

@src/main/api/types.ts
@src/main/state/settings.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create display logic utility module</name>
  <files>src/main/utils/displayLogic.ts</files>
  <action>
Create new file `src/main/utils/displayLogic.ts` with two functions:

1. `selectPrimaryLimit(sessionLimit, weeklyLimit)` - returns `{ limit, type }`:
   - Default to session
   - If weeklyLimit.percentage > 90:
     - Calculate weekly remaining capacity in session-equivalent units
     - Use formula: `weeklyRemainingInSessions = weeklyRemaining / (weeklyLimit.total / 10)`
     - If weeklyRemainingInSessions < sessionRemaining, return weekly
   - Handle edge cases: validate total > 0, default to session on error

2. `determineTrayIconColor(sessionLimit, weeklyAllModels, weeklySonnet)` - returns `'green' | 'yellow' | 'red'`:
   - Get max percentage from ALL three limits
   - If max >= 90 return 'red'
   - If max >= 70 return 'yellow'
   - Otherwise return 'green'

Export both functions. Import UsageLimit type from '../api/types'.

See RESEARCH.md Pattern 1 and Pattern 2 for reference implementation.
  </action>
  <verify>
- File exists at src/main/utils/displayLogic.ts
- TypeScript compiles: `npm run typecheck` passes
- Functions are exported
  </verify>
  <done>
Display logic utility with selectPrimaryLimit and determineTrayIconColor functions ready for use
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix auto-start to skip in development mode</name>
  <files>src/main/state/settings.ts</files>
  <action>
Modify `setAutoStart` function in src/main/state/settings.ts:

1. Check if running in development: `const isDev = !app.isPackaged`
2. If isDev, skip calling `app.setLoginItemSettings` entirely (just update store)
3. If production, proceed with existing logic

This prevents development runs from creating registry entries pointing to node_modules/electron/dist/electron.exe.

Also update `initSettings` to skip syncing with Windows in dev mode (registry state is unreliable in dev).

Add console.log to indicate when auto-start is skipped in dev mode.
  </action>
  <verify>
- TypeScript compiles: `npm run typecheck` passes
- In dev mode (`npm run dev`), toggling auto-start should NOT change Windows registry
- Run app in dev, check that console shows "[Settings] Skipping auto-start in dev mode"
  </verify>
  <done>
Auto-start only configures Windows registry in production builds, preventing electron.exe registry entries
  </done>
</task>

</tasks>

<verification>
1. `npm run typecheck` passes with no errors
2. displayLogic.ts exports selectPrimaryLimit and determineTrayIconColor
3. settings.ts includes dev mode check before app.setLoginItemSettings
</verification>

<success_criteria>
- Display logic utility module created with both functions
- Auto-start setting protected from creating dev registry entries
- All TypeScript compiles cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/05-display-logic-fixes/05-01-SUMMARY.md`
</output>
