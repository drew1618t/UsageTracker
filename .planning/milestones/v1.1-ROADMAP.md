# Milestone v1.1: Polish & Fixes

**Status:** ✅ SHIPPED 2026-01-31
**Phases:** 5
**Total Plans:** 2

## Overview

Session-first display with smart weekly priority, fix auto-start bug. Focus on display logic improvements and development bug fix.

## Phases

### Phase 5: Display Logic & Fixes

**Goal:** Implement session-first display logic and fix auto-start bug
**Depends on:** v1.0 foundation (Phases 1-4)
**Plans:** 2 plans

Plans:
- [x] 05-01: Create display logic utility and fix auto-start dev mode
- [x] 05-02: Integrate display logic into tray and popup

**Details:**

**Requirements:**
- DISPLAY-01: Session limit shown by default in popup and tooltip
- DISPLAY-02: Weekly limit takes priority only when >90% used AND more limiting in final 10%
- DISPLAY-03: Tray icon color reflects ANY limit hitting thresholds (early warning)
- FIX-01: Auto-start launches app correctly (not bare electron.exe)

**Success Criteria:**
1. Popup and tooltip show session limit by default
2. Weekly limit only displays when >90% AND calculated to be more limiting than remaining session capacity
3. Tray icon turns yellow/red if ANY limit (session or weekly) hits threshold
4. App launches correctly on Windows startup (not bare electron.exe window)
5. Manual launch still works as before

**Technical Notes:**
- Display logic: 1 session ≈ 10% weekly, so final 10% weekly = ~1 session worth
- Icon color: Keep existing threshold logic but apply to all limits independently
- Auto-start: Registry entry protected from dev mode

---

## Milestone Summary

**Key Decisions:**
- Session-first display with weekly override only when >90% AND more limiting
- Tray icon color based on ANY limit hitting threshold (independent evaluation)
- Dev mode detection via app.isPackaged
- Inline selectPrimaryLimit in renderer (can't import from main process)
- Fixed display order with dynamic highlighting

**Issues Resolved:**
- Auto-start registering electron.exe instead of app path (fixed via dev mode protection)
- Display logic not prioritizing session limit (now session-first by default)

**Issues Deferred:**
- Settings click handler in tray context menu (placeholder, out of scope)

**Technical Debt Incurred:**
- Code duplication of selectPrimaryLimit between main and renderer (intentional, documented)

---

_For current project status, see .planning/ROADMAP.md (next milestone)_
_Archived: 2026-01-31_
